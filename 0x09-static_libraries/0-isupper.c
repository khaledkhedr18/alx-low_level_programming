#include <stdio.h>
#include "main.h"

/**
* _isupper - Entry point
* Return: always 0.
* @c: testing placeholder
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}

}
