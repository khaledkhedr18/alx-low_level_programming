#include <stdio.h>
#include "main.h"

/**
* mul - Entry point
* Return: always 0.
* @a: testing placeholders
* @b: testing placeholders
*/

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);

}
